#itertools

from itertools
accumulate() #for prefix sums
product() #cartesian product
permutations(); p, r #r-length tuples
combinations() p, r #r-length tuples, no repeated elements
combinations_with_replacement() p, r #r-length tuples with repeated elements

from collections import Counter
counter = Counter(arr)
top_three = counter.most_common(3)

from math
acos(); asin(); atan() #for inverse trig
cos(); sin(); tan() #for trig
facotiral()
log()
log10()
